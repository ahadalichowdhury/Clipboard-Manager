#include <stdio.h>

// This is a dummy main function to satisfy the linker
int main(int argc, char *argv[]) {
    // The real main function is in main.swift
    // This is just to satisfy the linker
    return 0;
}
